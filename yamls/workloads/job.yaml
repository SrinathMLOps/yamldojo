apiVersion: batch/v1
kind: Job
metadata:
  name: data-migration
spec:
  completions: 1
  backoffLimit: 4
  template:
    spec:
      containers:
        - name: migrate
          image: myorg/migrate:1.0
          command: ["./migrate.sh"]
      restartPolicy: Never
